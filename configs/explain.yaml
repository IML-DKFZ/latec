# @package _global_

defaults:
  - _self_
  - data: organmnist3d.yaml # choose datamodule with `test_dataloader()` for evaluation
  - logger: null
  - paths: default.yaml
  - xai_method: voxel.yaml
  - extras: default.yaml
  - hydra: default.yaml

task_name: "explain"

tags: ["dev"]
time: ${now:%Y-%m-%d}_${now:%H-%M-%S}

# passing checkpoint path is necessary for evaluation
ckpt_path: "data/model_weights/"
